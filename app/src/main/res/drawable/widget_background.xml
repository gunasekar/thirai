<?xml version="1.0" encoding="utf-8"?>
<!-- Widget container: the brand's warm near-black surface, softly rounded with
     a faint warm outline so it reads as a single premium panel on any wallpaper. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#17130C" />
    <corners android:radius="24dp" />
    <stroke
        android:width="1dp"
        android:color="#332A1A" />
</shape>
